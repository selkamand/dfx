# n_distinct() generates useful errors

    Code
      n_distinct()
    Condition
      Error in `n_distinct()`:
      ! argument "x" is missing, with no default
    Code
      n_distinct(mean)
    Condition
      Error in `n_distinct()`:
      ! `x` must be a vector or a data.frame, not an object of class: [function]

