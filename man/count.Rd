% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{count}
\alias{count}
\title{Count}
\usage{
count(data, columns, sort = FALSE, name = "n", drop = TRUE)
}
\arguments{
\item{data}{a data.frame.}

\item{columns}{names of columns whole levels to tally (character vector).}

\item{sort}{sort by tallies in descending order.
If \code{FALSE} table is sorted by columns (flag)}

\item{name}{name of column describing frequencies.
Where \code{name} clashes with \code{columns} it will be
dot-prefixed until clash is removed. See details (string).}

\item{drop}{exclude zero-counts from factor levels not present in data.}
}
\value{
a data.frame describing counts per level.
}
\description{
Tally the frequency of each level in a column
(or combination of levels in several column).
}
\details{
It is not guaranteed that the tally column name defined by \code{name}
will be the true name in the resulting data.frame.
If the \code{name} argument clashes with a column in \code{columns} then name will
be prefixed with '.' until unique.

The fact we cannot guarantee tally column name means there are only two safe ways extract this tally column.
\enumerate{
\item Extract the final column of the count data.frame
\item Lookup the 'n' attribute of the count data.frame which describes the true name of the tally column.
}
}
\examples{
# Create a sample data frame
df <- data.frame(
  x = 1:5,
  y = letters[1:5],
  z = rnorm(5)
)

# Count columns 'x' and 'z'
count(df, c("x", "z"))

# Set name of tally column
count(df, c("x", "z"), name = "tally")

}
