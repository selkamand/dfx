% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversions.R
\name{convert_vector_to_match_target}
\alias{convert_vector_to_match_target}
\title{Convert type to match target}
\usage{
convert_vector_to_match_target(
  x,
  target,
  failure = c("error", "keep_original"),
  error_prefix = "conversion failure: "
)
}
\arguments{
\item{x}{a vector whose type you want to convert.}

\item{target}{a vector whose type you want to convert x to.}

\item{failure}{When conversion errors should we throw an \code{error} or \code{keep_original} type.}

\item{error_prefix}{a string to prefix on the error. This can be used to
indicate calling scope since we set \code{call.=FALSE} when calling stop since
the trycatch scope is pretty uninformative}
}
\value{
vector \code{x} with a class matching target
}
\description{
Convert vector x to target (must be a vector)
On failure if failure = \code{error} throw an error. If \code{keep_original} just return the original x
}
\details{
When \code{target} is a factor, its levels are ignored. Levels will be derived only from the contents of \code{x}.

When target is a POSIXct datetime the timezone will be set to match that of \code{target}
}
